mcp类似于ai连接外部工具的接口标准

包含三个核心概念：
1.Resurces(资源)
2.Prompts(提示模板)
3.Tools(工具)


MCP是ai与外部工具的通信协议，
先是ai大模型出现后，需要一些应用对ai暴露一些接口来让ai使用应用的某些功能，
最开始是不同模型有不同的通信字段，这样应用要对不同的模型进行适配，
所以Anthropic就提出了MCP协议这个格式，期望统一模型对应用调用的字段格式，
让应用只需要对一套格式进行适配就就可以让所有模型进行调用